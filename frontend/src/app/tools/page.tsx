import { Check, ExternalLink, Sparkles, Star } from 'lucide-react';

const tools = [{ name: 'OpenCowork', description: '轻量级 AI 工作助手，支持屏幕监控与可扩展的 Skills 工作流。', features: ['屏幕实时监控', 'Skills 工作流系统', '支持 OpenAI、Claude 与 Ollama', 'Tauri + Rust'], url: 'https://github.com/mypengpengli/OpenCowork', tags: ['开源', '免费', 'AI助手'], stars: 5 }];

export default function ToolsPage() {
  return <div className="page-shell py-7 sm:py-10"><section className="max-w-3xl"><p className="text-sm font-semibold text-brand">工具目录</p><h1 className="mt-2 text-3xl font-bold text-slate-950">推荐工具</h1><p className="mt-3 text-base leading-7 text-slate-600">只收录已经实际使用、值得继续关注或方便读者验证的工具。</p></section><div className="mt-8 grid gap-4 lg:grid-cols-2">{tools.map((tool) => <article key={tool.name} className="surface-card p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><Sparkles className="size-5 text-brand" /><h2 className="text-xl font-bold text-slate-900">{tool.name}</h2></div><div className="mt-3 flex flex-wrap gap-1.5">{tool.tags.map((tag) => <span key={tag} className="rounded-full bg-slate-100 px-2 py-1 text-xs font-medium text-slate-600">{tag}</span>)}</div></div><span className="inline-flex items-center gap-1 text-sm font-medium text-amber-600"><Star className="size-4 fill-current" />{tool.stars}</span></div><p className="mt-5 text-sm leading-6 text-slate-600">{tool.description}</p><ul className="mt-5 grid gap-2 text-sm text-slate-700">{tool.features.map((feature) => <li key={feature} className="flex items-center gap-2"><Check className="size-4 text-emerald-600" />{feature}</li>)}</ul><a href={tool.url} target="_blank" rel="noopener noreferrer" className="mt-6 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-control bg-brand text-sm font-medium text-white transition hover:bg-brand-strong">访问项目<ExternalLink className="size-4" /></a></article>)}</div><p className="mt-8 text-center text-sm text-slate-500">有值得收录的工具？请发送邮件至 <a href="mailto:306100898@qq.com" className="font-medium text-brand hover:underline">306100898@qq.com</a>。</p></div>;
}
